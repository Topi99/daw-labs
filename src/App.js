import React from 'react';
import { BrowserRouter } from 'react-router-dom';

const App = () => {
  return(
    <main>
      <nav>
        nav
      </nav>
      <BrowserRouter>

      </BrowserRouter>
    </main>
  );
}

export default App;